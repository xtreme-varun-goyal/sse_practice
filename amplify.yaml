version: 1
frontend:
  phases:
    preBuild:
      commands:
        - echo "No build step"
    build:
      commands:
        - echo "Static site"
  artifacts:
    baseDirectory: .
    files:
      - '**/*'
  cache:
    paths: []
